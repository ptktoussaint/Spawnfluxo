PORT=3000
ADMIN_PASSWORD=troque-esta-senha

# Em produção (ex.: Render), aponte para o disco persistente montado.
# Em desenvolvimento local, deixe vazio para usar a pasta ./data do projeto.
DATA_DIR=

# true se o app rodar atrás de um proxy/load balancer (ex.: Render, Railway)
TRUST_PROXY=true
